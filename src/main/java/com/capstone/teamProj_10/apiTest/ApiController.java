package com.capstone.teamProj_10.apiTest;

/*
@RestController
@RequestMapping("/api")
public class ApiController {

    private final RegionService regionService;

    @Autowired
    public ApiController(RegionService regionService) {
        this.regionService = regionService;
    }

    @GetMapping("/search")
    public String searchAddress(@RequestParam String sido, @RequestParam String sigungu) {
        try {
            return regionService.searchAddress(sido, sigungu);
        } catch (Exception e) {
            e.printStackTrace();
            return "Error occurred while fetching data.";
        }
    }
}
*/
